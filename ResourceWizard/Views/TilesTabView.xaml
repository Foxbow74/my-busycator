<UserControl x:Class="ResourceWizard.Views.TilesTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:StoreableView="clr-namespace:ResourceWizard.StoreableView" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="5*"/>
		</Grid.ColumnDefinitions>
		<ListBox ItemsSource="{Binding Tiles}" DisplayMemberPath="Tile" x:Name="lb" SelectedIndex="0" Margin="10,10,0,10"/>
		<StoreableView:TileInfoSelectorView Grid.Column="1" DataContext="{Binding ElementName=lb, Path=SelectedItem}"/>
	</Grid>
</UserControl>
